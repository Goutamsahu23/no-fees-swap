# Option A — generate from deployment JSONs (run from no-fee-swap-ui/):
#   npm run env:print
#   npm run env:write
#
# Option B — copy this file and fill addresses manually:
#   copy .env.example .env.local   (Windows)
#   cp .env.example .env.local     (macOS/Linux)

NEXT_PUBLIC_RPC_URL=http://127.0.0.1:8545
NEXT_PUBLIC_CHAIN_ID=31337

NEXT_PUBLIC_NOFEESWAP=0x
NEXT_PUBLIC_NOFEESWAP_DELEGATEE=0x
NEXT_PUBLIC_OPERATOR=0x
NEXT_PUBLIC_TOKEN0=0x
NEXT_PUBLIC_TOKEN1=0x
